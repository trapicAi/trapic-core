# Copy to .env and fill in real values:
#   cp .env.example .env

# MariaDB (for docker-compose.mariadb.yml)
MARIADB_ROOT_PASSWORD=change-me-root
MARIADB_PASSWORD=change-me-trapic

# PostgreSQL (for docker-compose.postgres.yml)
PG_PASSWORD=change-me-trapic
